# The memql commit every CI lane checks out as the ../memql sibling.
#
# This file is the SINGLE source of truth for that pin. Workflows must not
# name a ref themselves -- they reach the sibling through
# .github/actions/checkout-memql, which reads this file. The guard job in
# ci.yml fails if any workflow checks out znasllc-io/memql directly.
#
# WHY PINNED. memql is being split into ~29 nested modules (memql#3228). No
# static cockpit go.mod is green both before and after a given module lands
# (see the truth table in go.mod), and there is no atomic cross-repo merge.
# Pinning buys the window: cockpit keeps building against a known memql while
# that module lands, then ONE cockpit PR moves this pin and adds that module's
# require + replace in the same commit.
#
# TO BUMP: change the sha below, add the newly-landed module's `require` and
# `replace` to go.mod, and run `go mod tidy` -- all in one commit.
#
# Currently pinned to memql main after the app-session credential class
# landed (memql#4863) -- the identity node mints class="app_session" for a
# delegated run and the Library's byte routes admit that class as the user
# it names, which is what makes appsession.Library.Pull / .Push work at all.
# Before this the back-channel collapsed into class="service_account", a
# class that surface pins off deliberately, and every pull answered 401.
# memql-cockpit#371 is the cockpit half of that.
#
# WHAT THIS BUMP IS NOT. It does NOT carry the wire that epics memql#5096
# (tools on the ModelCall family, `message` control and `result` on app
# sessions, app descriptors on Register) and memql#5103 (ModelPullStart /
# Progress / End) own. Neither had merged when this moved. The cockpit
# halves of both are implemented against a narrow seam -- see
# internal/worker/appsession/session.go's controlPrompt and
# internal/worker/inference/pull.go -- so landing them is a pin bump plus
# the field mapping, not a rewrite. Do not read the presence of harness
# clients or the inference package as proof the wire exists.
#
# component/harness LOST its go.mod between the previous pin and this one,
# so its `require` and `replace` came OUT in the same commit. That is row 4
# of the truth table in go.mod read backwards: a replace with a require and
# no go.mod on the far side is "ambiguous import", and the fix is to delete
# both directives rather than to add anything. The remaining set is what
# `go mod tidy` converged on and is unchanged otherwise.
#
# ADVERTISEMENT STILL NEEDS NO PROTO CHANGE, which is worth knowing before
# reading the diff for one: models ride Register's EXISTING labels map as
# `model:<id>` and `runtime:<kind>`, per the engine's
# integrations/agent/worker/model_routing.go, and the params / quant / tools
# attributes this repository now advertises are more text inside that same
# map value. Only the SERVING half of tool calling depends on memql#5096.

# BUMPED 2026-09-08 to memql main at the merge of the guided cockpit install
# (memql PR #5229, epic memql#5218). THIS bump carries the wire the previous
# paragraphs said it did not: the ModelPullStart / Progress / End / Cancel
# family (memql#5103) and the ModelProbe family (memql#5146), the tool fields
# on the ModelCall family and the app descriptors on Register (memql#5096),
# the hardware inventory on Register and Heartbeat (memql#5146), the `role`
# field on the access record (memql#5181), and -- new in that epic -- Ping on
# the server envelope and Pong on the client envelope (memql#5225, D11 of
# docs/superpowers/specs/2026-09-08-cockpit-install-wizard-design.md in the
# engine repository). The runner answers the Ping and runs the ModelPull arm
# (internal/worker/modelpull.go) against this pin; the ModelProbe arm is still
# not written. `go mod tidy` moved the indirect set to what the engine now
# requires (openai-go in, go-openai out, gjson up) and nothing else.
# Native inference coordination (memql PR #5238): adds the working-context
# field on ModelCallParams; this worker maps it to Ollama num_ctx. Model active
# parameter counts still travel in Register labels. Pinned to the actual
# engine main merge, including first-machine onboarding and chat targeting.
# Fleet discovery and Ask reliability (memql PR #5244): shared authorized
# catalog, streaming readiness, stable context allocation, and persistent
# node liveness. Pin the exact tested engine main merge.

# BUMPED 2026-09-14 to memql main at the merge of memql PR #5424 (memql#5393,
# the wire half of the app door completion, epic memql#5391; the cockpit half
# is epic memql-cockpit#436). It carries `level` on AppSessionStart and
# ModelCallStart, the `model` and `effort` the app REPORTED on AppSessionEnd,
# and `effort` beside the existing `model` on ModelCallUsage -- design D8/D9
# of the engine's docs/superpowers/specs/2026-09-13-app-session-recording-
# and-learning-program-design.md, landed proto-first as that record orders.
# The merge also descends from memql PR #5423 (the SDK's serialized
# worker-stream send, memql#5351), so a later pin for epic
# memql-cockpit#425 moves forward from here rather than past it.
#
# THE LESSON OF THE PREVIOUS BUMP, written down so it is not repeated. The
# 2026-09-08 bump above carried four memql#5096 fields --
# AppSessionControl.prompt, AppSessionStart.response_schema_json,
# AppSessionEnd.result_json and Register.app_descriptors -- and no cockpit
# code read any of them for a week (memql-cockpit#444): follow-ups arrived
# empty, no session was asked for a schema, no structured answer reached the
# engine. A bump that lands a field lands its mapping in the same PR; grep for
# the stand-in (the "until memql#NNNN lands" comment) before calling it done.
#
# `go mod tidy` moved github.com/znasllc-io/memql/core to a direct require
# (internal/worker/harness imports core/airoute, the engine's own level
# vocabulary) and google.golang.org/grpc with it (the grpc/codes and
# grpc/status imports were already direct), and dropped go.sum lines the
# dependency bumps had left behind. No module landed or left between the two
# pins, so the replace block is unchanged.
#
# EPIC memql-cockpit#425 NEEDS NO BUMP OF ITS OWN, and the reason is worth
# knowing before reading that epic's diff. Its one engine dependency is the
# send lock of memql PR #5423 (memql#5351, audit finding C-1): the SDK's
# sdk/go/worker.Connection.Send serializes every write on the WorkerService
# stream under one mutex, with the agent's sticky first error, and Close takes
# CloseSend under the same lock; Stream(), the one way around it, is gone.
# That merge (memql 977c6ea5) is an ancestor of the pin below. This
# repository's Connection routes every Send* helper through the SDK's Send
# (TestEveryWorkerWriteGoesThroughConnectionSend), so no code here changed
# for it -- but a pin that went back past 977c6ea5 would quietly take the
# lock away.
456623efe52136f70c540f6b907067f3846e5901
