name: sbom

on:
  release:
    types: [published]
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'go.mod'
      - 'go.sum'

permissions:
  contents: read

# Generates a CycloneDX SBOM for the cockpit Go module on every
# release (and on go.mod / go.sum changes for main). The artifact
# is uploaded to the release when triggered by a release event and
# stashed as a workflow artifact otherwise. Mirrors the memql SBOM
# workflow so the supply-chain story is uniform.
#
# memql-cockpit ships two binary variants from one module (headless
# default + `computeruse` build tag). The SBOM produced here covers the
# module-level dependency graph; per-binary SBOMs that reflect the
# build-tagged subset are a separate exercise tracked in #65 (TBD).

jobs:
  sbom:
    name: cyclonedx-gomod
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout memql-cockpit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: memql-cockpit
      - name: Checkout memql (sibling for the ../memql replace directive, pinned)
        # Pinned via .github/memql-pin -- see that file and go.mod. memql is
        # mid module-split (memql#3228) and an unpinned main breaks cockpit the
        # moment any nested go.mod lands.
        uses: ./memql-cockpit/.github/actions/checkout-memql
        with:
          # Dependabot's restricted token can't read the sibling repo;
          # MEMQL_SIBLING_TOKEN (Dependabot secret) covers that, with a
          # fallback to the default token on normal runs.
          token: ${{ secrets.MEMQL_SIBLING_TOKEN || github.token }}
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: '1.26.6'
          cache: true
          cache-dependency-path: memql-cockpit/go.sum
      - name: install cyclonedx-gomod
        run: go install github.com/CycloneDX/cyclonedx-gomod/cmd/cyclonedx-gomod@v1.10.0
      - name: generate SBOM
        working-directory: memql-cockpit
        run: |
          mkdir -p sbom
          cyclonedx-gomod mod -licenses -json -output sbom/memql-cockpit.cdx.json
          cyclonedx-gomod mod -licenses -output sbom/memql-cockpit.cdx.xml
      - name: upload SBOM as workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: sbom
          path: memql-cockpit/sbom/
          retention-days: 90
      - name: attach SBOM to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        with:
          files: |
            memql-cockpit/sbom/memql-cockpit.cdx.json
            memql-cockpit/sbom/memql-cockpit.cdx.xml
